# 

(V1): BERT-NER pre-trained model + regex and gazetteer
This results in a wholly deterministic model in the sense that there is no training as such that is required. We are utilzing a rule-based approach to be able to produce the NER-tags. Based on regex-rules that we have set for the 